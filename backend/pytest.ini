[pytest]
testpaths = tests
# make the `app` package importable no matter how pytest is invoked
# (bare `pytest` does not add the working dir to sys.path; `python -m pytest` does)
pythonpath = .
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
